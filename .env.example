# Copy to .env (or export in your shell). Loaded by plot2spec.config.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides:
# P2S_MODEL=claude-sonnet-5
# P2S_CACHE_DIR=.cache/claude
# P2S_RENDER_DPI=200
